# 미래형학교체육 '스마트점핑'(Smart-Jumping)

> 참고 사이트: 신개념 교실체육 스마트점프 (jjinbro.co.kr/sj)
> 확정 사항: 구글 시트 백엔드 · YouTube 임베드 · 정적 프론트

---

## 1. 아키텍처

```
[브라우저 프론트]  ──fetch──▶  [Google Apps Script Web App]  ──▶  [Google Sheets]
  HTML/CSS/JS                    doGet / doPost (JSON API)          9개 시트 = DB
  YouTube IFrame API             CacheService 캐싱                   Drive 내 1개 파일
```

- 프론트: 순수 HTML/CSS/JS (빌드 도구 없음). 로컬 파일로 열어도 동작.
- 배포: Apps Script HTML Service 로 통째로 서빙 (별도 호스팅비 0원, URL 하나로 배포)
- 영상: YouTube IFrame Player API → 플레이리스트 연속 재생 제어

### 왜 이 조합인가
| 항목 | 이유 |
|---|---|
| Apps Script | 시트 인증이 자동 해결. 서버·도메인·요금 불필요 |
| YouTube 임베드 | 썸네일/재생시간 자동 획득, 스토리지 비용 0 |
| 캐싱 필수 | Apps Script 왕복이 0.5~2초. 영상 목록은 CacheService(6h) + localStorage 이중 캐시 |

---

## 2. 구글 시트 스키마 (탭 9개)

| 시트 | 컬럼 |
|---|---|
| `categories` | 대분류ID, 대분류명, 대분류순서, 소분류ID, 소분류명, 소분류순서, N뱃지 |
| `videos` | 영상ID, 대분류ID, 소분류ID, 제목, youtube_id, 재생시간, 조회수, 등록일, 노출여부 |
| `users` | 아이디, 비번해시, 이름, 소속, 지역, 권한(교사/관리자), 가입일, 상태 |
| `classes` | 수업ID, 소유자, 수업년월, 지역, 학교, 학년, 반, 정원, 메모 |
| `attendance` | 수업ID, 학생명, 날짜, 출결(출/결/지/조) |
| `equipment` | 소유자, 교구명, 대리점명, 새제품수량, 중고제품수량, 수정일 |
| `schedules` | 소유자, 그룹명, 차시, 영상ID목록(쉼표구분) |
| `textbooks` | 권차, 제목, 표지URL, 뷰어링크, 뱃지 |
| `playlists` | 소유자, 담은영상ID목록, 수정일 |

---

## 3. 화면 목록

### 공통 셸
- 좌측 사이드바: 로고 / 사용자 카드(이름 + **▶수업시작** + 로그아웃) / 메뉴 4종
- 상단 GNB: 대분류 6개 + 우측 실시간 시계
- 메뉴: 스마트점핑 · 수업관리 · 마이페이지(정보수정/교구보유현황/프로그램스케줄/회원탈퇴) · 온라인교재

### 콘텐츠 라이브러리 (메인)
- 검색바 → 소분류 필터칩(전체 + N뱃지) → 4열 카드 그리드
- 카드 = 체크박스 + 썸네일(재생시간·조회수 오버레이) + 제목 + `[[대분류] > [소분류]]`
- 우하단 플로팅 **영상담기** 버튼 (선택 개수 표시)

### 수업 플레이어 (핵심 차별점)
- 담은 영상 전체화면 연속 재생
- 재생목록 사이드 패널, 이전/다음, 순서 드래그, 반복
- 교실 프로젝터 기준 대형 UI

### 수업관리
- 기간 필터(년/월) + 조건 검색(학교명+지역) + `전체 N / 검색 M`
- 표: 번호·수업년월·지역·학교·학년·반·정원 + 출석부/보기/수정/삭제 아이콘
- 등록 모달, 페이지네이션

### 마이페이지
- 정보수정 / 교구보유현황(교구명·대리점명·새제품·중고제품) / 프로그램스케줄 / 회원탈퇴

### 온라인교재
- 표지 5열 그리드 + 검색

---

## 4. 단계별 작업 — 전 단계 완료 (2026-08-22)

| 단계 | 내용 | 상태 |
|---|---|---|
| **P0** | 시트 스키마 + Apps Script API + 더미데이터 | ✅ 영상 124건 자동 생성 |
| **P1** | 공통 셸 (사이드바·GNB·시계·반응형) | ✅ |
| **P2** | 카테고리·검색·필터칩·카드 그리드 + 캐싱 | ✅ |
| **P3** | 영상담기 → 수업 플레이어 연속 재생 | ✅ |
| **P4** | 로그인/회원 + 수업관리 + 출석부 | ✅ |
| **P5** | 교구보유·스케줄·온라인교재·회원탈퇴 | ✅ |

배포 방법은 [README.md](README.md) 참고.

### 검증 결과
- 백엔드 통합 테스트 **49/49 통과**
- 프론트 전 화면 실브라우저 동작 확인, 콘솔 에러 0건
- 모바일 375px 가로 스크롤 없음

### 남은 일 (데이터 채우기)
- [ ] `videos` 탭 `youtube_id` 열에 실제 유튜브 ID 입력 → 썸네일·재생 자동 연결
- [ ] `재생시간` 열 입력 (또는 YouTube Data API 키로 자동화)
- [ ] `textbooks` 탭 표지URL·뷰어링크
- [ ] 로고 이미지 / 브랜드 색상 확정

---

## 5. 미리 알아야 할 제약

1. **속도** — Apps Script 응답이 느립니다(0.5~2초). 영상 목록은 반드시 캐싱하고, 첫 로딩에 스켈레톤 UI를 넣습니다.
2. **동시 쓰기** — 조회수·플레이리스트 저장은 `LockService` 로 잠금. 조회수는 즉시 반영 대신 배치 누적 권장.
3. **보안** — 시트에 비밀번호 평문 저장 금지(해시 필수). 진짜 계정 보안이 필요하면 Google 로그인으로 전환하는 편이 안전합니다.
4. **규모** — 시트 수천 행까지는 문제없습니다. 영상이 1만 건을 넘어가면 구조 변경 필요.
5. **재생시간·썸네일** — YouTube ID만 넣으면 썸네일은 자동(`img.youtube.com`). 재생시간은 YouTube Data API 키가 있으면 자동, 없으면 시트에 수기 입력.

---

## 6. 시작 전 필요한 것

- [ ] 구글 시트를 새로 만들지, 기존 시트를 쓸지
- [ ] 실제 영상 목록 (YouTube 링크 + 분류) — 없으면 더미로 진행
- [ ] 로고 이미지 / 브랜드 색상 (없으면 참고 사이트 톤인 보라~파랑으로 진행)
- [ ] YouTube Data API 키 (선택 — 재생시간 자동화용)

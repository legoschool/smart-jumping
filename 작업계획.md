# 미래형학교체육 '스마트점핑'(Smart-Jumping) — 작업 기록

**데모 사이트 : https://legoschool.github.io/smart-jumping/**
**저장소 : https://github.com/legoschool/smart-jumping**

---

## 1. 아키텍처

```
[데모]  브라우저 ── local-backend.js ──▶ localStorage
[풀스택] 브라우저 ── google.script.run ──▶ Apps Script ──▶ 구글 시트 9탭
```

프론트 코드는 두 버전이 100% 동일합니다. `web/local-backend.js` 가
`google.script.run` 과 똑같은 인터페이스를 localStorage 위에 구현해서
백엔드만 갈아끼웁니다.

---

## 2. 완료 상태 (2026-08-22)

| 단계 | 내용 | 상태 |
|---|---|---|
| P0 | 시트 스키마 + Apps Script API | ✅ |
| P1 | 공통 셸 (사이드바·GNB·시계·반응형) | ✅ |
| P2 | 카테고리·검색·필터칩·카드 그리드 + 캐싱 | ✅ |
| P3 | 영상담기 → 수업 플레이어 연속 재생 | ✅ |
| P4 | 로그인/회원 + 수업관리 + 출석부 | ✅ |
| P5 | 교구보유·스케줄·온라인교재·회원탈퇴 | ✅ |
| P6 | 디자인 전면 개편 | ✅ |
| P7 | 실제 유튜브 영상 59건 연동 | ✅ |
| P8 | GitHub Pages 게시 | ✅ |
| P9 | 사용법 화면(#/guide) + 수업 구성 튜토리얼, 라운드·소프트 디자인 리프레시 | ✅ |
| P10 | 안내 캐릭터를 제공받은 여자 어린이 학생 1인으로 교체 (임의 캐릭터 4종 제거) | ✅ |
| P11 | 첫 화면 상단에 대표 콘텐츠 영상 히어로 (무음 자동재생·반복·소리 토글) | ✅ |

---

## 3. 유튜브 영상 검증 절차

임의의 ID를 넣으면 깨진 임베드가 되므로 3단계로 걸렀습니다.

1. **생존 확인** — oEmbed API. 61건 중 2건이 비공개/삭제로 탈락 → 59건
2. **임베드 허용 확인** — watch 페이지의 `playableInEmbed`. 59건 전부 통과
3. **재생시간 실측** — `lengthSeconds`. 미확보 0건

썸네일은 `maxresdefault` 53건 + 없는 6건은 `hqdefault` 자동 대체 → 커버리지 100%.

---

## 4. 검증 결과

| 항목 | 결과 |
|---|---|
| 백엔드 통합 테스트 | 53 / 53 통과 |
| 라이브 사이트 썸네일 | 6개 영역 59/59 로드, 저해상도 잔존 0 |
| 라이브 사이트 재생 | `PlayerState.PLAYING`, duration 376초 = 저장값 06:16 일치 |
| 콘솔 에러 | 0건 |
| 모바일 375px | 가로 스크롤 없음 |

---

## 5. 작업 중 잡은 버그

1. **`$$` 헬퍼 소실** — 빌드 스크립트의 `String.replace()` 가 치환 문자열의
   `$$` 를 `$` 로 축약. 함수형 치환으로 교체하고 빌드 시 검증 단계 추가.
2. **썸네일 저해상도 폴백 누락** — `onerror` 만으로는 유튜브가 404 대신
   placeholder 를 200 으로 주는 경우를 못 잡음. 크기 검사 기반으로 교체.

> 측정 아티팩트였던 것: 자동화 브라우저 패널이 숨겨져 있으면 `document.hidden=true`
> 라 `loading="lazy"` 이미지가 로드되지 않음. 실사용에는 영향 없음.

---

## 6. 남은 일

- [ ] 구글 시트에 데이터 넣기 → `스마트점핑_DB.xlsx` 를 **파일 → 가져오기 → 스프레드시트 바꾸기**
- [ ] Apps Script 6개 파일 붙여넣고 웹앱 배포 (README 참고)
- [ ] `textbooks` 탭 표지URL·뷰어링크 채우기
- [ ] 로고 이미지 / 최종 브랜드 색상 확정
- [ ] 실제 스마트점핑 자체 제작 영상으로 `videos` 탭 교체
      (현재는 공개 유튜브 영상으로 구성 — 카테고리명도 그에 맞춰 조정해 둠)

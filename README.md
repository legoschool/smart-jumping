# 미래형학교체육 '스마트점핑' (Smart-Jumping)

교실에서 바로 쓰는 학교체육 수업 영상 플랫폼. 수업 전에 영상을 담아두고, 교실에서는 재생 버튼 하나로 한 차시를 진행합니다.

**▶ 데모 사이트 : https://legoschool.github.io/smart-jumping/**

| 계정 | 비밀번호 | 권한 | 보이는 것 |
|---|---|---|---|
| `teacher` | `1234` | **관리자** | 전체 메뉴 + **모든 교사의 수업 6건** |
| `test` | `1234` | 교사 | 전체 메뉴 + 자기 수업 3건 |
| `teststu` | `1234` | 학생 | 영상 · 교재 · 정보수정 · 탈퇴 |

---

## 두 가지 버전

| | 데모 (GitHub Pages) | 풀스택 (Apps Script + 구글 시트) |
|---|---|---|
| 위치 | `index.html` | `apps-script/` |
| 백엔드 | localStorage | 구글 시트 |
| 데이터 공유 | 브라우저 1대 | 계정 전체 |
| 회원가입·탈퇴 | ○ (이 브라우저 한정) | ○ |
| 비용 | 0원 | 0원 |

**프론트 코드는 두 버전이 100% 동일합니다.** `web/local-backend.js` 가 `google.script.run` 과 똑같은 인터페이스를 localStorage 위에 구현해서, 프론트는 자기가 어느 백엔드에 붙어 있는지 몰라도 됩니다.

---

## 담긴 영상

**실제 유튜브 공개 영상 104건.** 전부 아래를 통과한 것만 넣었습니다.

- oEmbed API 로 생존 확인 → 삭제·비공개 4건 자동 제외
- watch 페이지의 `playableInEmbed` 로 **외부 재생 허용** 확인 → 104건 전부 통과
- 재생시간은 `lengthSeconds` 실측값
- 썸네일 커버리지 100%. `maxresdefault` 가 없는 11편은 목록을 미리 구워 두어 처음부터 `hqdefault` 를 부른다 (`npm run verify:thumb`)

| 영역 | 세부 분류 | 영상 |
|---|---|---|
| 명상 | 호흡 명상 · 마음챙김 명상 · **어린이 명상** | 11 |
| 시간타이머 | 휴식영상 타이머 · 카운터 타이머 (1·3·5·10분) | 18 |
| 준비운동 \| 타바타 | 몸풀기&스트레칭 · 타바타&기초체력 · 기초체력 추천 조합 · 점핑 근력운동 · **정리운동·쿨다운** · **레크리에이션 놀이** | 32 |
| 스마트점핑 | 급수 친구들 1~3단계 · 쌩쌩이·2단뛰기 · **음악줄넘기** · 지도자 필수교육 | 23 |
| 비트 트레이닝 | 동요 율동 · 체조 메들리 · **K-POP 키즈댄스** | 9 |
| Killing Time Zone | 나라별 국기맞추기 · 숨은그림·틀린그림 찾기 · **넌센스 퀴즈** | 11 |

총 **6영역 23분류 104편**. 굵게 표시한 5개 분류가 이번에 새로 생겼습니다.

튜토리얼 화면은 이 영상들을 그대로 후보로 씁니다 — 몸풀기(몸풀기 & 스트레칭) → 스마트점핑(전 분류) → 비트 트레이닝 → 마무리(정리운동 · 쿨다운 / 명상) 순으로 한 편씩 고르면 수업 한 차시가 됩니다.

> 영상은 모두 각 유튜브 채널의 원본을 임베드로 재생합니다. 저작권은 각 채널에 있으며, 이 저장소는 영상을 재배포하지 않습니다.

---

## 화면

| 화면 | 기능 |
|---|---|
| **첫 화면** | 상단에 **대표 콘텐츠 영상**(스마트 점핑 고급 6차시)이 무음 자동재생 · 반복. 그 아래로 바로 영상 목록. 로그인은 담기·수업관리가 필요할 때만 모달로 |
| **사용법 · 튜토리얼** | 안내 캐릭터가 화면별 조작을 짚어 주고, **실제 영상을 골라 4차시 수업을 만들어 보는 튜토리얼**까지 |
| 로그인 / 회원가입 | 아이디 규칙·비밀번호 확인·중복 검사 |
| 스마트점핑 | 검색 · 영역/세부분류 필터 · **정렬(조회수·재생시간·제목)** · 카드 그리드 · 페이지네이션 · **영상담기** |
| 수업 플레이어 | 담은 영상 전체화면 연속 재생 · 재생목록 · 이전/다음 · 반복 · 재생오류 자동 건너뛰기 |
| 수업관리 | 기간(년·월) · 검색조건(학교명+지역 / 학교명 / 지역) · 페이지네이션 · 등록/수정/삭제 |
| **학급 커리큘럼** | 학급에 프로그램 스케줄을 연결 → 표의 **▶** 로 차시를 골라 바로 수업 시작 |
| **출석부** | **날짜별 회차 누적** · 지난 회차 조회 · 새 날짜엔 최근 명단 자동 승계 · 누적 출석률 |
| **회원관리** (관리자) | 권한 변경 · 계정 정지/복구 · 비밀번호 초기화 · 보유 수업/교구/스케줄 집계 |
| 마이페이지 | 정보수정 · 교구보유현황 · 프로그램스케줄 · 회원탈퇴 |
| 권한 제어 | 관리자는 전 교사 수업 조회(담당 열 표시) / 교사는 자기 것만 / 학생은 영상·교재만. URL 직접 접근도 차단 |
| 계정별 데이터 | 담은 영상·수업·교구·스케줄은 계정마다 따로 보관 (학생도 담기·수업시작 가능) |
| 온라인교재 | 교재 그리드 · 검색 |
| **영상 즐겨찾기** | 카드의 ★ 로 담고 상단 **★ 즐겨찾기** 에서 모아보기 (계정별) |
| **내 출결** | 학생·학부모가 본인 출결과 출석률을 조회 |
| **수업 리포트** | 학급 정보 + 커리큘럼 + 회차별 출결표를 인쇄 / PDF 저장 |

### 수업 중 단축키
`Space` 재생/일시정지 · `←` `→` 이전/다음 · `Esc` 종료

---

## 구글 시트 연동판 설치

### 1. 시트 채우기 — 둘 중 하나

**A. XLSX 가져오기 (Apps Script 없이, 가장 빠름)**

1. `스마트점핑_DB.xlsx` 다운로드
2. 구글 시트 열기 → **파일 → 가져오기 → 업로드**
3. 파일 선택 후 **스프레드시트 바꾸기** → 가져오기

10개 탭이 그대로 들어갑니다. (영상 104건 · 카테고리 23 · 수업 6 · 출석 27 · 교재 12)

**B. Apps Script 로 자동 생성**

`apps-script/00_설정.gs` 의 `TARGET_SHEET_ID` 에 본인 시트 ID를 넣고 `초기설정_실행()` 실행.
시트 URL 의 `/d/` 와 `/edit` 사이 문자열이 시트 ID입니다.

```
https://docs.google.com/spreadsheets/d/  ← 여기부터  →  /edit
                                       1AbCdEf...
```

시트에서 **확장 프로그램 → Apps Script** 로 열었다면 `TARGET_SHEET_ID` 를 비워둬도 그 시트를 자동으로 채웁니다.

### 2. 웹앱 배포

<https://script.google.com> → 새 프로젝트 → 아래 6개 파일을 붙여넣습니다.

| 편집기에서 만들 파일 | 종류 |
|---|---|
| `00_설정` | 스크립트 |
| `01_초기설정` | 스크립트 |
| `02_API` | 스크립트 |
| `index` | **HTML** |
| `css` | **HTML** |
| `chars` | **HTML** |
| `js` | **HTML** |

> `index` / `css` / `chars` / `js` 는 반드시 **HTML 파일**로. 확장자는 붙이지 않습니다. 기본 `코드.gs` 는 지웁니다.
> `chars` 는 안내 캐릭터 이미지를 base64 로 구워 둔 자동 생성 파일입니다 (`node tools/gen-chars.js`).

**배포 → 새 배포 → 웹 앱** / 실행 계정 **나** / 액세스 **링크가 있는 모든 사용자**

---

## assets/ — 사이트에 쓰는 그림·영상

| 경로 | 내용 |
|---|---|
| `assets/char/*.webp` | 안내 캐릭터 6포즈 (전신 `.webp` + 얼굴 `-face.webp`). 배경을 따내고 리사이즈한 가공본 |
| `assets/hero/lesson.mp4` | 첫 화면 대표 콘텐츠 영상 (1280×720 · 1분 51초 · 5.0MB) |
| `assets/hero/poster.webp` | 그 영상의 포스터. 영상이 오기 전/못 올 때 자리를 지킨다 |

캐릭터와 포스터는 `node tools/gen-chars.js` 로 `apps-script/chars.html` 에 base64 로 구워 넣습니다. **영상만 파일 참조**(5MB라 인라인 불가)이므로, GitHub Pages 데모에서는 재생되고 **Apps Script 판에서는 포스터 그림이 대신 보입니다.** Apps Script 에서도 재생하려면 `js` 파일의 `HERO_VIDEO` 를 절대 URL 로 바꾸세요.

---

## 시트 구조

| 탭 | 열 |
|---|---|
| `categories` | 대분류ID, 대분류명, 대분류순서, 소분류ID, 소분류명, 소분류순서, N뱃지 |
| `videos` | 영상ID, 대분류ID, 소분류ID, 제목, **youtube_id**, 재생시간, 조회수, 등록일, 노출여부 |
| `users` | 아이디, 비번해시, 이름, 소속, 지역, 권한, 가입일, 상태 |
| `classes` | 수업ID, 소유자, 수업년월, 지역, 학교, 학년, 반, 정원, 메모, **스케줄그룹** |
| `attendance` | 수업ID, 학생명, **날짜**, 출결 — (수업, 날짜) 조합마다 한 회차 |
| `equipment` | 소유자, 교구명, 대리점명, 새제품수량, 중고제품수량, 수정일 |
| `schedules` | 소유자, 그룹명, 차시, 영상ID목록 |
| `textbooks` | 권차, 제목, 표지URL, 뷰어링크, 뱃지 |
| `playlists` | 소유자, 담은영상ID목록, 수정일 |
| `favorites` | 소유자, 즐겨찾기영상ID목록, 수정일 |

### 영상 추가
`videos` 탭에 행을 추가하고 `youtube_id` 에 영상 ID를 넣으면 끝입니다.

```
https://www.youtube.com/watch?v=AbCdEfGh123   →   AbCdEfGh123
https://youtu.be/AbCdEfGh123                  →   AbCdEfGh123
```

### 카테고리 변경
`categories` 탭을 고치면 상단 메뉴와 필터칩이 그대로 따라옵니다. **영상이 0건인 분류는 화면에 나타나지 않으므로**, 분류를 미리 다 적어두고 영상을 채워가도 화면이 깨지지 않습니다.

### ⚠ 시트를 직접 고친 뒤에는
영상·카테고리는 6시간 캐싱됩니다. 즉시 반영하려면 Apps Script 에서 **`캐시비우기`** 를 실행하세요.

---

## 알아둘 점

1. **속도** — Apps Script 왕복은 0.5~2초입니다. 서버 6시간 + 브라우저 이중 캐싱을 걸어두었고 로딩 중에는 스켈레톤이 뜹니다.
2. **로그인 보안** — 비밀번호는 SHA-256 해시로 저장합니다. 교내용 수준이지 은행급은 아닙니다. 실제 학교 계정을 받아 운영할 계획이면 구글 로그인으로 전환하는 편이 안전합니다.
3. **데모 사이트** — 백엔드가 없습니다. 데이터는 방문자 브라우저에만 저장되고 서로 공유되지 않습니다. 콘솔에서 `sjReset()` 을 실행하면 초기화됩니다. 온라인교재는 원문 뷰어가 없어 카드를 누르면 안내만 뜹니다. 나머지 화면은 시트판과 똑같이 동작합니다.
4. **동시 사용** — 시트 쓰기에는 `LockService` 잠금이 걸려 있습니다.
5. **재생 오류** — 임베드가 막힌 영상을 만나면 안내를 띄우고 5초 뒤 자동으로 다음 영상으로 넘어갑니다.

---

## 검증

| 항목 | 결과 |
|---|---|
| 백엔드 통합 테스트 | **125 / 125 통과** |
| 프론트 기능 점검 | **51 / 51 통과** (셸·라이브러리·플레이어·수업관리·마이페이지·교재) |
| 권한 제어 | **22 / 22 통과** (교사·관리자·학생 메뉴 노출 + 라우트 가드) |
| 계정별 데이터 격리 | **13 / 13 통과** (담은 영상·수업·교구·스케줄 계정마다 독립) |
| 권한별 데이터 범위 | 관리자 6건 / 교사 3건 / 학생 0건 — 남의 수업 수정·삭제 거부 |
| 색 대비(WCAG AA) | 본문 전 항목 4.5:1 이상 — 미달 4건 수정 |
| 유튜브 영상 | **104건 전수** 생존·임베드 허용·재생시간 확인 |
| 썸네일 | 커버리지 100% |
| 정렬 | 조회수·재생시간(↑↓)·제목 4종 순서 검증 |
| 학급 커리큘럼 | 연결 5건/미연결 1건 표시 → 차시 선택 → 4편 연속재생 확인 |
| 출석부 누적 | 회차 3→4건 증가, 지난 회차 8명 보존, 누적 출석률 산출 |
| 회원관리 | 권한 승격·정지·복구·비밀번호 초기화 + 마지막 관리자 강등 차단 |
| 게스트 모드 | 로그인 없이 영상 열람·재생 / 담기·즐겨찾기·보호메뉴는 로그인 유도 |
| 사용법 · 튜토리얼 | 4단계 선택 → 수업표 생성 → 바로 시작(플레이어 4편) · 수업에 담기(게스트는 로그인 유도) 확인 |
| 안내 캐릭터 | 6포즈 배경 제거 후 WebP 160KB, 12종 전부 인라인 — 빌드 검사 항목에 포함 |
| 대표 콘텐츠 영상 | 12.4MB → 5.0MB 재인코딩, 무음 자동재생 · 반복 · 소리 토글 · Range 스트리밍 확인 |
| 즐겨찾기 | 계정별 분리, 해제 시 목록 반영, 정렬·검색과 동시 동작 |
| 내 출결 · 리포트 | 학생 본인 기록만 조회, 교사는 남의 학급 리포트 거부 |
| 실제 재생 | 연속 2편 `PlayerState.PLAYING` 확인, 재생시간 실측치 일치 |
| 프론트 | 6개 영역 · 전 페이지 · 담기 → 연속재생 브라우저 실동작 확인 |
| 콘솔 에러 | 0건 — 썸네일 폴백이 만들던 404 11건까지 제거 |
| 출석 회차 날짜 | 데모는 방문 시점 기준으로 밀어 넣는다 (최근 회차 = 오늘, 간격 3일·7일 유지) |
| 회원가입 → 로그인 | 신규 계정 `demo01` 생성 후 로그인, 권한 교사로 부여 |
| 튜토리얼 | 4단계 선택 → 15분 38초 4편 → 담기(계정별 저장 확인) · 바로 시작(플레이어 진입) |
| 모바일(375px) | 가로 스크롤 없음 (`scrollWidth 375 = innerWidth`) |

---

## 라이선스

코드는 MIT. 임베드되는 유튜브 영상의 저작권은 각 채널에 있습니다.
